{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "构建stargo",
      "type": "shell",
      "command": "go",
      "args": ["build", "-v", "-o", "${workspaceFolder}/bin/stargo", "."],
      "options": { "cwd": "${workspaceFolder}" },
      "group": "build"
    }
  ]
}